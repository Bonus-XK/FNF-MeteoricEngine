# 基线对话核对：FNF引擎集成Lua图形化编程
# 会话日志：/Users/raidbounce/.dsh/sessions/--Users-raidbounce-Documents--/session-73257323-3a61-41c6-b0b9-dbd3a459ea28/session.jsonl.zstd (3197206 bytes)
# 解压命令：zstd -dc <日志> > /tmp/baseline.jsonl  → 8379429 bytes / 9140 行

## 标题事件（命中基线）
"title":"FNF引擎集成Lua图形化编程"
"title":"角色与任务 你是一位精通 Haxe/"

## 该对话对界面定下的硬约束（原文摘录，截断到 240 字符窗口）
视觉复核抓到**两处布局缺陷**（我自己看图发现，不是靠摘要）：\n\n1. **预览层代码行与底部提示行重叠**（22 行 × 21px 超出面板可用高度，\"滚轮/PgUp/PgDn 翻页\"压在最后一行代码上）；\n2. **状态条文字宽度 700 会钻进按钮区**（按钮从 x=728 起，文字可到 764）。\n\n修掉这两处，并把按键提示缩短以避免同类问题："}}}}
视觉复核抓到**两处布局缺陷**（我自己看图发现，不是靠摘要）：\n\n1. **预览层代码行与底部提示行重叠**（22 行 × 21px 超出面板可用高度，\"滚轮/PgUp/PgDn 翻页\"压在最后一行代码上）；\n2. **状态条文字宽度 700 会钻进按钮区**（按钮从 x=728 起，文字可到 764）。\n\n修掉这两处，并把按键提示缩短以避免同类问题："},{"type":"tool-call","id":"call_00_yfjii3LjzTStEU1qZgJK3196

## 结论
- 约束 = 三面板 + 令牌化 + 「高亮条先 add」层级契约 + 状态条不得钻进按钮区。
- 与本轮 6 项修复无冲突（未改令牌、未改高亮条 add 顺序、未动底栏布局常量）。
- 「快捷键提示文案超宽」确认为其遗留项（原文只说"把按键提示缩短"，实际文案仍 ~64 字符）→ 本轮按用户选择不修，已登记。

## 原文摘录（该对话自述的界面缺陷修复）
视觉复核抓到**两处布局缺陷**（我自己看图发现，不是靠摘要）： 1. **预览层代码行与底部提示行重叠**（22 行 × 21px 超出面板可用高度，"滚轮/PgUp/PgDn 翻页"压在最后一行代码上）； 2. **状态条文字宽度 700 会钻进按钮区**（按钮从 x=728 起，文字可到 764）。 修掉这两处，并把按键提示缩短以避免同类问题：
