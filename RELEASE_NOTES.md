# Meteoric Engine v1.1.1

> 基于 Psych Engine 0.7.1h 深度魔改的 FNF 引擎。v1.1.1 以 **联机真双人** 为核心，带来局域网 1v1 实时对战、房间自动发现、模组皮肤跨端同步，并完成大量稳定性 / 模组兼容 / 三端构建修复。

---

## ✨ 版本亮点

**Meteoric Engine 1.1.1** —— 联机功能完整落地并从「双人同打一侧」升级为**真双人各唱半边谱面**：两侧谱面同时滚动、人声与动画各归其位；新增局域网房间自动发现（免输 IP）；无模组玩家也能看到对方模组角色皮肤；结算界面重写（KE 命中散点图 + 对方成绩对比）；版本校验索引升级为 2。

---

## 🌐 联机对战（全新）

- **局域网 1v1 实时对战**：主菜单恢复「联机」入口，TCP 房主即服务器（端口 27750），严格 1v1、满员拒绝
- **本地判定 + 全量同步**：HIT / MISS / PRESS / RELEASE 增量广播；对方按键条、血条、分数、连击、判定统计实时同步
- **暂停同步**：单方暂停双端同步；暂停与结算期间持续轮询网络；修复 PAUSE/RESUME 回声导致的「无法继续游玩」死锁；联机禁用重开
- **血条攻防**：己方 Hit 推己方条、Miss 扣血，对方 Miss 按损失量 60% 回血
- **昵称 / 输入**：中文昵称 UTF-8 修复；输入框重构（future.ttf 显示 + 原生 IME 输入 + 自绘光标）；输入聚焦时屏蔽游戏键位；全界面鼠标操作
- **房间大厅**：两页制四区布局（玩家列表 / 房间信息 / 选曲 / 状态提示）；选曲携带难度与 mod 与判定规则，客户端强制跟随；回大厅连接保持，可直接再来一局

## 🎤 联机真双人（v1.1.1 核心）

- **各唱半边谱面**：房主打 player1（BF 位），玩家打 player2（Dad 位）；客户端镜像人声（Vocals ↔ OpponentVocals）
- **左侧谱面恢复滚动**：不再是「双方都唱 BF 侧、对方侧变空气」；对方按键命中左侧音符、Dad 唱歌、对方 Miss 播 Miss 动画、长按完整判定
- **chartSeq 精确消费**：HIT / MISS 携带音符序号，接收端按序号消费（`findOnlineOppNoteBySeq`，视觉副本覆盖时回退基准音符）
- **布局与相机**：双端统一标准单机布局（自己箭头在右/中、对方在左/两侧）；中间滚动偏移按物理位置修正；相机跟随当前演唱角色
- **加载预载**：加载界面按角色映射预载双方贴图，进曲不黑屏/闪图

## 📡 局域网房间自动发现

- **UDP 广播发现**（27751）：房主每 1s 心跳广播（房间码 / 房主昵称 / 人数 / 是否已满），广播至 `255.255.255.255` + 回环 + 本机局域网 IP（单机双开可发现）
- **「发现房间」页面**：首页第三项入口；实时列表、4 秒未收到心跳自动移除、满房置灰、**点击行直连**（免输 IP）；离开房间自动停止广播

## 🧩 模组皮肤跨端同步（CHARSYNC）

- 选角确认时把模组角色资源（角色 JSON / 图集 / 血条图标）按 60KB 分块同步到对端 `mods/` 兜底路径——**无模组玩家也能看到对方模组皮肤**
- **出站队列 + 每帧续发**：非阻塞 socket 缓冲满（Blocked）视为背压，不再误判断线
- 资源收集遍历**全部已安装模组**；过滤资源不全的半残角色；同步前完整性校验；进曲锁定我方角色所在模组
- 「开始对局」需等对方资源同步完成

## 🧾 结算与判定

- **KE 命中散点图**（NoteHitGraph）：横轴音符时间、纵轴命中偏移、4 条判定窗口线、Mean 均值（移植自 Kade Engine 1.8）
- **结算界面重写**：曲目信息 + 判定统计两列 + 本局数据（分数 / 最高连击 / 准确率 / 新纪录）+ 大版散点图
- Marvelous 改**金黄色**、遮挡与间距修复；结算时自动隐藏 HUD 判定侧边栏
- **联机结算**：精简版 + **对方成绩栏**实时刷新；「继续」直接回房间大厅
- **Early / Late 指示器**（Psych-Forever 移植）：偏早/偏晚命中显示对应判定图（普通 + 像素全套）
- **金色 Combo**：全 Marvelous/Sick 时切换金色计数，Good 以下或 Miss 恢复；修复 Combo 词方向键偏移不生效
- **三帧胜利小图标**（OSEngine 移植）：血量 >80% 切胜利帧、<20% 切劣势帧；原版 2 帧图标保留 `-pe` 后缀兼容

## 🛠️ 稳定性 · 性能 · 兼容

- 暂停→返回主菜单闪退根治（转场冻结 + LuaJIT `pushing nil` 修复）
- QT / FlxAnimate 动画修复：ATLAS 坐标随降采样缩放、`playAnim` 强制重启、DCE 保护（`@:keep`）、`addLuaSprite` 官方语义
- 内存优化：未用缓存每 60s 清理（useCount 守卫）、角色图集销毁、回放/重开同曲 100% 死循环修复、StrumNote 缺资源兜底
- FunkinMix shader 白/黑屏修复（FlxRuntimeShader 官方对齐 + 相机边界公开访问）；runHaxeCode `Map` 变量预处理
- 编谱面板重写（`ChartingPanel` / `ChartWidgets`，独立纹理根除「悬浮串亮」）；多引擎导出（CNE / Psych 0.6.3）
- **多层级崩溃日志**：黑匣子心跳 / 阶段簿 / Haxe 未捕获 / 原生信号栈 / Java 层；Seiun 式完整系统报告
- 安卓：后台冻结（暂停 + 音频）、Java 崩溃落盘、重开回溯 NPE、onTrimMemory 重复定义修复
- 66mod HScript 兼容：变量声明、`camFollow` 注入、SScript inline 静态反射修复
- 像素舞台下落箭头恢复 NEAREST 硬边；背景学生重开表情重置；GF 快速重开修复

## 🚀 版本与发布

- 版本号 **1.1.0 → 1.1.1**，版本校验索引 **1 → 2**（`gitVersion.txt` 同步更新，已升级玩家不再被误判旧版）
- GitHub 仓库补齐 `.gitignore`（Gradle/Android 构建产物、`.DS_Store`、`build/`/`bin/`/`obj/`、`export/`）
- 三端自动构建产物：**Windows x64 / x86 · macOS x86_64 / arm64 · Android arm64**

---

## 🖥️ 平台

- Windows（x64 / x86）/ macOS（x86_64 / arm64）/ Android（arm64）三端由 GitHub Actions 自动构建并发布
- 本地构建：`compile-windows.sh` / `compile-mac.sh` / `compile-android.sh`
