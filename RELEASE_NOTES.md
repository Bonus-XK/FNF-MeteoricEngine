# Meteoric Engine v1.1.0

> 基于 Psych Engine 深度魔改的 FNF 引擎，历经五轮重构，加入全新 Replay 回放系统、安卓移动端完整适配与 0.6.3 模组兼容。

---

## ✨ 版本亮点

**Meteoric Engine 1.1.0** —— 五轮 Reboot 重构的集大成版本：全新的按键注入式回放系统、HUD 子系统重构、安卓端完整触控体验、Psych 0.6.3 模组兼容。

---

## 🎮 玩法与手感

- **新版音符判定（KE 判定）**：防乱按扣分、严格命中窗口、Bad 以下评分微量扣血；旧版判定保留为「PE 判定」可切换
- **KE 引擎小图标跳动**（设置开关）
- **自动游玩重写**：真实 100% 准确率（非固定判定），支持加载预演
- **无限轮回**：曲目完成后时间回溯重新开始
- **对方推条**：对手像玩家一样按键加血
- **小图标与血量条强制绑定**，图标跟随血量条左右滑动（经典 FNF 手感）
- **快速重开 + 时间回溯**：重开时箭头像录像倒带一样飞回起点（变速回溯，接近终点逐渐减速）

## ⚡ 性能与加载

- **加载界面重构**：多线程加载，超大谱面（55670 音符）约 2 秒加载完成，跳过冗余过渡动画
- **提前渲染（预渲染）设置**，自动游玩加载时预演
- **音符渲染批处理**：堆叠音符统一批处理命中，消除卡顿
- **完成/退出曲目后自动清理 RAM**（全界面生效）
- **启动速度优化**，`-release` 增量编译

## 🖥️ 界面全面重构（统一设计语言）

- **圆角磨砂玻璃面板**贯穿全部界面：主菜单、自由选歌、故事模式、暂停、结算、游玩设置、按键设置、脚本管理、制作人员、旧版更新提示等
- **鼠标/触控/键盘三套操作互不冲突**：悬浮高亮、滚轮/滑动选择、A 键确认
- **中文化**：全部界面与设置项中文翻译，全字体渲染（移除旧 alphabet）
- **系统鼠标光标**，圆形磨砂玻璃返回按钮
- **FPS 计数器重构**：内存/CPU 峰值显示、GB 单位、可集成到窗口标题栏
- **连击数突破 9999**，Sick/9999 图片不堆叠
- **主界面彩蛋**：输入 `meforever` 触发字母乱飞等特效
- **音量悬浮窗重构**：拖动条 + 平滑加减
- **旧版 1.0.4 界面保留**（alphabet 风格）

## 🛠️ 编辑器与系统

- **编谱界面（Chart Editor）全面重构**：现代右侧面板、双方小图标、中文界面
- **游戏内报错系统重写**：报错窗口显示在游戏内，大幅降低闪退率，附崩溃测试入口
- **Lua/Hscript 脚本系统增强**：import 导入库、class 支持与继承、界面自定义端口（主菜单等）
- **脚本管理**：游戏内开关 lua/hx 脚本（关闭加 `.disabled` 后缀），支持当前谱面目录脚本
- **自定义 HUD 界面**：音符/时间条/血量条/评分位置自由调节（设置内模拟预览 + 重置到默认）
- **成就系统重写**：全中文命名与描述、触发方式显示

## 📱 安卓端（本次重点）

- **虚拟按键（virtualpad）触控体系**：A 确认、滑动自然滚动、L/P/C 快捷键、移除 B 键
- **滑键支持**：手指不松手从一键滑到另一键也判定按下
- **侧滑/返回键 → 暂停**（而非退出桌面）
- **后台自动暂停**，恢复后游戏状态正常
- **长条锁定**，音符显示与模组箭头兼容
- **崩溃界面**：安卓端弹窗式错误提示 + AB 键操作
- **模组路径** `/sdcard/meteoric/mods`，相册不出现贴图
- 修复 16KB 对齐闪退、启动闪退、界面偏移、读谱失败、FPS 不显示等

## 🎞️ Replay 回放系统（v2 重写）

- **全新按键注入式回放**：录制按键按下/抬起事件，回放时注入正常输入路径——判定、strum 动画、长按、幽灵点击全部按真实游玩复现
- 结算界面「回放本局」一键回看；自由选歌 P 键打开回放列表（播放/删除）
- 回放中暂停、重开、跳时间全部可用
- 旧版回放文件自动兼容

## 🧩 模组兼容（Psych 0.6.3 / 0.7.1h）

- **0.6.3 兼容模式**：箭头贴图格式、小图标取色、时间条/血量条渲染逻辑
- 兼容 Spritemap 新旧格式（Adobe Animate 2020 spritemap1）
- 大量模组（Tenebris、QT、weddream 等）适配修复

## 🐛 稳定性修复

- **修复游玩中 UI 突然消失**：HUD 可见性权威看门狗 + 内存清理不再销毁使用中的贴图（useCount 守卫）
- **暂停界面**：资源缺失不再卡死、触屏直接点选菜单项
- **上帝模式正常记分**：分数/Miss/准确率/全连照常记录与保存，仅保留"不会死"
- 修复第二次加载曲目崩溃、箭头不显示/变红、血条发白等问题
- 快速重开人物/Event 换人重载、回溯后时间条同步

## 🖥️ 平台

- Windows（x64）/ macOS / Android 三端可编译（`compile-windows.sh` / `compile-mac.sh` / `compile-android.sh`）
